Add your About section image here as `about.jpg`.

Recommended dimensions: ~1200×800 for good quality on desktop.

The homepage About component references `/about.jpg` and falls back to a remote image if missing.
